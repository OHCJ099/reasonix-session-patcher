# Quick Deploy

1. 解压本压缩包到任意目录（推荐 `G:\codex-session-patcher-reasonix`）。
2. 双击 `start_reasonix_session_patcher.bat`。
3. 浏览器会自动打开 `http://127.0.0.1:47833/`。
4. 可选：双击 `install_desktop_shortcut.bat` 创建桌面快捷方式。

首次启动会自动创建 `.venv` 并安装 FastAPI 等后端依赖；前端 `dist` 已随包构建好，通常不需要 Node/npm。
